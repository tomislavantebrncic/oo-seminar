@model IEnumerable<WebApplication.Models.MedicalExamination>

@{
    ViewBag.Title = "Index";
}

<h2>Čekaonica</h2>

<p>
    @Html.ActionLink("Dodaj novi pregled", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExaminationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExaminationType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExaminationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExaminationType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>

        <td>
            @Html.ActionLink("Pregledaj", "Edit", new { id = item.Id })
            &nbsp;          &nbsp;
            @Html.ActionLink("Povijest bolesti", "Details", new { id = item.Id })
        </td>
    </tr>
}

</table>
